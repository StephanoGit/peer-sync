python3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOFpython3 - <<'EOF'
import socket
from zeroconf import Zeroconf, ServiceInfo

zc = Zeroconf()
infos = zc.get_service_info("_sharefile._tcp.local.", "_sharefile._tcp.local.")
print("Services registered on this machine:")
for name in zc._services.get("_sharefile._tcp.local.", {}).keys():
    print(f"  {name}")
zc.close()
EOF
